// lib/utils/constants.dart
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blue;
  static const completed = Colors.green;
  static const delete = Colors.red;
}

class AppStrings {
  static const appTitle = 'Todo App';
  static const noTasks = 'No tasks yet! Add one below.';
  static const addTask = 'Add a new task';
  static const addButton = 'Add';
}
